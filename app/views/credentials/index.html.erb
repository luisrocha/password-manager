<% content_for :title, "Password Manager" %>
<%
  primary_button_class = "inline-flex min-h-11 items-center justify-center rounded-md border border-teal-700 bg-teal-700 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-teal-800"
  secondary_button_class = "inline-flex min-h-11 items-center justify-center rounded-md border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50"
  table_header_class = "border-b border-slate-200 px-4 py-3 text-left text-xs font-semibold uppercase text-slate-600"
  table_cell_class = "border-b border-slate-200 px-4 py-3 align-top"
  mobile_label_class = "text-xs font-semibold uppercase text-slate-500"
%>

<main class="mx-auto w-full max-w-6xl px-4 py-6 sm:px-6 lg:px-8">
  <header class="mb-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
    <div class="space-y-2">
      <h1 class="text-3xl font-semibold text-slate-950">Password Manager</h1>
    </div>

    <nav class="flex flex-col gap-3 sm:flex-row sm:items-center" aria-label="Credential actions">
      <%= link_to "Add Item", new_credential_path, class: primary_button_class %>
      <%= link_to "Import CSV", import_credentials_path, class: secondary_button_class %>
      <%= button_to "Lock Vault", lock_path,
        method: :delete,
        form: { class: "inline-form", data: { controller: "lock", action: "submit->lock#clear" } },
        class: "#{secondary_button_class} w-full sm:w-auto" %>
    </nav>
  </header>

  <% if notice.present? %>
    <p class="mb-4 rounded-md border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-900"><%= notice %></p>
  <% end %>
  <% if alert.present? %>
    <p class="mb-4 rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800"><%= alert %></p>
  <% end %>

  <section class="mb-5 rounded-lg border border-slate-200 bg-white p-4 shadow-sm">
    <%= form_with url: credentials_path, method: :get, local: true, html: { class: "flex flex-col gap-3 sm:flex-row" } do |f| %>
      <%= f.search_field :q, value: @query, placeholder: "Search by name or domain", aria: { label: "Search credentials by name or domain" }, class: "block min-h-11 w-full rounded-md border border-slate-300 px-3 py-2 text-base shadow-sm focus:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-600/20" %>
      <div class="flex gap-3">
        <%= f.submit "Search", class: "#{primary_button_class} grow sm:grow-0" %>
        <%= link_to "Clear", credentials_path, class: "#{secondary_button_class} grow sm:grow-0" %>
      </div>
    <% end %>
  </section>

  <section class="rounded-lg border border-slate-200 bg-white shadow-sm">
    <div class="border-b border-slate-200 px-4 py-3">
      <h2 class="text-sm font-semibold text-slate-950">Stored Items (<%= @credentials.count %>)</h2>
    </div>

    <% if @credentials.empty? %>
      <p class="px-4 py-8 text-sm text-slate-600">No stored items yet.</p>
    <% else %>
      <div class="divide-y divide-slate-200 md:hidden">
        <% @credentials.each do |credential| %>
          <article
            class="px-4 py-4"
            data-controller="credential-reveal"
            data-credential-reveal-encrypted-payload-value="<%= credential.encrypted_secret_payload %>"
          >
            <div class="font-semibold text-slate-950"><%= credential.name.presence || "Untitled" %></div>
            <div class="mt-1 break-words text-sm text-slate-600"><%= credential.domain.presence || "No domain" %></div>

            <dl class="mt-4 space-y-3">
              <div class="flex items-center justify-between gap-3">
                <dt class="<%= mobile_label_class %>">Category</dt>
                <dd><span class="inline-flex rounded-full bg-teal-50 px-2.5 py-1 text-xs font-semibold uppercase text-teal-800"><%= credential.category %></span></dd>
              </div>
              <div class="flex min-h-10 items-center justify-between gap-3">
                <dt class="<%= mobile_label_class %>">Username</dt>
                <dd class="break-all text-sm text-slate-950" data-credential-reveal-target="username">Decrypting...</dd>
              </div>
              <div class="flex min-h-10 items-center justify-between gap-3">
                <dt class="<%= mobile_label_class %>">Password</dt>
                <dd class="inline-flex items-center gap-2">
                  <span class="break-all text-sm text-slate-950" data-credential-reveal-target="password">Hidden</span>
                  <button
                    type="button"
                    class="icon-button table-icon-button reveal-secret-button"
                    aria-label="Reveal password"
                    title="Reveal password"
                    data-credential-reveal-target="passwordButton"
                    data-action="credential-reveal#revealPassword"
                  ></button>
                </dd>
              </div>
              <div class="flex min-h-10 items-center justify-between gap-3">
                <dt class="<%= mobile_label_class %>">Notes</dt>
                <dd class="inline-flex items-center gap-2">
                  <span class="break-all text-sm text-slate-950" data-credential-reveal-target="notes">Hidden</span>
                  <button
                    type="button"
                    class="icon-button table-icon-button reveal-secret-button"
                    aria-label="Reveal notes"
                    title="Reveal notes"
                    data-credential-reveal-target="notesButton"
                    data-action="credential-reveal#revealNotes"
                  ></button>
                </dd>
              </div>
            </dl>

            <div class="mt-4 flex items-center gap-2">
              <%= link_to edit_credential_path(credential),
                class: "icon-button table-icon-button icon-pencil action-link",
                aria: { label: "Edit credential" },
                title: "Edit credential" do %>
                <span class="visually-hidden">Edit credential</span>
              <% end %>
              <%= button_to credential_path(credential),
                method: :delete,
                form: { class: "inline-form" },
                data: { turbo_confirm: "Delete this credential?" },
                class: "icon-button table-icon-button icon-trash action-danger",
                aria: { label: "Delete credential" },
                title: "Delete credential" do %>
                <span class="visually-hidden">Delete credential</span>
              <% end %>
            </div>
          </article>
        <% end %>
      </div>

      <div class="hidden overflow-x-auto md:block">
        <table class="w-full min-w-[64rem] table-fixed border-collapse">
          <colgroup>
            <col class="w-[24%]">
            <col class="w-[11%]">
            <col class="w-[18%]">
            <col class="w-[18%]">
            <col class="w-[19%]">
            <col class="w-[10%]">
          </colgroup>
          <thead class="bg-slate-50">
            <tr>
              <th class="<%= table_header_class %>">Item</th>
              <th class="<%= table_header_class %>">Category</th>
              <th class="<%= table_header_class %>">Username</th>
              <th class="<%= table_header_class %>">Password</th>
              <th class="<%= table_header_class %>">Notes</th>
              <th class="<%= table_header_class %>">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @credentials.each do |credential| %>
              <tr
                data-controller="credential-reveal"
                data-credential-reveal-encrypted-payload-value="<%= credential.encrypted_secret_payload %>"
              >
                <td class="<%= table_cell_class %>">
                  <div class="font-semibold text-slate-950"><%= credential.name.presence || "Untitled" %></div>
                  <div class="mt-1 break-all text-sm text-slate-600"><%= credential.domain.presence || "No domain" %></div>
                </td>
                <td class="<%= table_cell_class %>">
                  <span class="inline-flex rounded-full bg-teal-50 px-2.5 py-1 text-xs font-semibold uppercase text-teal-800"><%= credential.category %></span>
                </td>
                <td class="<%= table_cell_class %>">
                  <span class="break-all text-sm text-slate-950" data-credential-reveal-target="username">Decrypting...</span>
                </td>
                <td class="<%= table_cell_class %>">
                  <span class="inline-flex items-center gap-2">
                    <span class="break-all text-sm text-slate-950" data-credential-reveal-target="password">Hidden</span>
                    <button
                      type="button"
                      class="icon-button table-icon-button reveal-secret-button"
                      aria-label="Reveal password"
                      title="Reveal password"
                      data-credential-reveal-target="passwordButton"
                      data-action="credential-reveal#revealPassword"
                    ></button>
                  </span>
                </td>
                <td class="<%= table_cell_class %>">
                  <span class="inline-flex items-center gap-2">
                    <span class="break-all text-sm text-slate-950" data-credential-reveal-target="notes">Hidden</span>
                    <button
                      type="button"
                      class="icon-button table-icon-button reveal-secret-button"
                      aria-label="Reveal notes"
                      title="Reveal notes"
                      data-credential-reveal-target="notesButton"
                      data-action="credential-reveal#revealNotes"
                    ></button>
                  </span>
                </td>
                <td class="<%= table_cell_class %>">
                  <div class="flex items-center gap-2 md:justify-start">
                    <%= link_to edit_credential_path(credential),
                      class: "icon-button table-icon-button icon-pencil action-link",
                      aria: { label: "Edit credential" },
                      title: "Edit credential" do %>
                      <span class="visually-hidden">Edit credential</span>
                    <% end %>
                    <%= button_to credential_path(credential),
                      method: :delete,
                      form: { class: "inline-form" },
                      data: { turbo_confirm: "Delete this credential?" },
                      class: "icon-button table-icon-button icon-trash action-danger",
                      aria: { label: "Delete credential" },
                      title: "Delete credential" do %>
                      <span class="visually-hidden">Delete credential</span>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% end %>
  </section>
</main>
