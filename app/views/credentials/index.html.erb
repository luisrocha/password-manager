<% content_for :title, "Password Manager" %>
<%
  primary_button_class = "inline-flex min-h-11 items-center justify-center rounded-md border border-teal-700 bg-teal-700 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-teal-800"
  secondary_button_class = "inline-flex min-h-11 items-center justify-center rounded-md border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50"
  search_icon_button_class = "icon-button search-icon-button shrink-0"
%>

<main class="mx-auto w-full max-w-6xl px-4 py-6 sm:px-6 lg:px-8">
  <%= turbo_stream_from Credential.broadcast_stream_name if @query.blank? %>

  <header class="mb-5 flex items-center justify-between gap-4 sm:items-end">
    <div class="min-w-0">
      <h1 class="text-3xl font-semibold text-slate-950">Password Manager</h1>
    </div>

    <details class="relative sm:hidden">
      <summary class="menu-summary icon-button menu-icon-button icon-menu hover:bg-slate-50">
        <span class="visually-hidden">Open credential actions</span>
      </summary>
      <nav class="absolute right-0 z-10 mt-2 grid w-48 gap-2 rounded-md border border-slate-200 bg-white p-2 shadow-sm" aria-label="Credential actions">
        <%= link_to "Add Item", new_credential_path, class: primary_button_class %>
        <%= link_to "Import CSV", import_credentials_path, class: secondary_button_class %>
        <%= link_to "Security", security_path, class: secondary_button_class %>
        <%= link_to "Connected apps", connected_apps_path, class: secondary_button_class %>
        <%= button_to "Lock Vault", lock_path,
          method: :delete,
          form: { class: "inline-form w-full", data: { controller: "lock", action: "submit->lock#clear" } },
          class: "#{secondary_button_class} w-full" %>
      </nav>
    </details>

    <nav class="hidden flex-col gap-3 sm:flex sm:flex-row sm:items-center" aria-label="Credential actions">
      <%= link_to "Add Item", new_credential_path, class: primary_button_class %>
      <%= link_to "Import CSV", import_credentials_path, class: secondary_button_class %>
      <%= link_to "Security", security_path, class: secondary_button_class %>
      <%= link_to "Connected apps", connected_apps_path, class: secondary_button_class %>
      <%= button_to "Lock Vault", lock_path,
        method: :delete,
        form: { class: "inline-form", data: { controller: "lock", action: "submit->lock#clear" } },
        class: "#{secondary_button_class} w-full sm:w-auto" %>
    </nav>
  </header>

  <% if notice.present? %>
    <p class="mb-4 rounded-md border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-900" data-extension-connect-target="notification"><%= notice %></p>
  <% end %>
  <% if alert.present? %>
    <p class="mb-4 rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" data-extension-connect-target="notification"><%= alert %></p>
  <% end %>

  <section class="mb-5 rounded-lg border border-slate-200 bg-white p-3 shadow-sm sm:p-4">
    <%= form_with url: credentials_path, method: :get, local: true, html: { class: "flex items-center gap-2" } do |f| %>
      <%= f.search_field :q, value: @query, placeholder: "Search by name or domain", aria: { label: "Search credentials by name or domain" }, class: "block min-h-11 w-full rounded-md border border-slate-300 px-3 py-2 text-base shadow-sm focus:border-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-600/20" %>
      <button type="submit" class="<%= search_icon_button_class %> icon-search" aria-label="Search" title="Search"></button>
      <%= link_to credentials_path, class: "#{search_icon_button_class} icon-x action-link", aria: { label: "Clear search" }, title: "Clear search" do %>
        <span class="visually-hidden">Clear search</span>
      <% end %>
    <% end %>
  </section>

  <%= render "credentials/index_list", credentials: @credentials %>
</main>
